import React from 'react';
import Button from '../Button';

function Demo() {
  return (
    <>
      <Button size="small" type="primary">large</Button>
      <Button size="default" type="primary">default</Button>
      <Button size="large" type="primary">large</Button>
    </>
  )
}

export default Demo;
